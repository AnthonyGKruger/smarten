import FiltersTitle from "./FiltersTitle";
import FiltersGrid from "./FiltersGrid";

const Filters = () => {
  return (
    <section>
      <FiltersTitle />
      <FiltersGrid />
    </section>
  );
};

export default Filters;
